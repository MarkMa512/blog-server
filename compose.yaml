# For dev purposes only
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=blog_server'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
